import { Box, Heading } from "@chakra-ui/react";

const Header = () => {
  return (
    <Box bg="teal.500" p={4}>
      <Heading color="white">My App</Heading>
    </Box>
  );
};

export default Header;
