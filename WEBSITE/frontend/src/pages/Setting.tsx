import { Box, Heading } from "@chakra-ui/react";

const Setting = () => {
  return (
    <Box p={3} pl={{ base: 16, md: 4 }}>
      <Heading fontSize={{ base: "xl", md: "2xl" }}>Setting</Heading>
    </Box>
  );
};

export default Setting;
