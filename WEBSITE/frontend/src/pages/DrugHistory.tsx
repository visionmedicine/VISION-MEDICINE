import { Box, Heading } from "@chakra-ui/react";

const DrugHistory = () => {
  return (
    <Box p={3} pl={{ base: 12, md: 4 }}>
      <Heading fontSize={{ base: "xl", md: "2xl" }}>Drug History</Heading>
    </Box>
  );
};

export default DrugHistory;
